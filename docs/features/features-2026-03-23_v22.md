# Features Implementadas — v22 (2026-03-23)

**Fecha:** 23 de Marzo, 2026
**Versión:** 22
**Hora:** ~22:43

---

## Polish UI — Pantalla Importar Ejecuciones CSV

### 1. Tablas ordenables por cualquier columna

- Nuevo hook `useSortable<T>`: recibe array de items, mantiene estado `{col, dir}`, retorna array ordenado + función `toggle`
- Componente `Th`: header clickable con iconos `ArrowUp` / `ArrowDown` / `ArrowUpDown` (Lucide) según estado activo
- Aplicado a: tabla de ejecuciones, tabla de trades, tabla de candidatos MANUAL

### 2. Orden por defecto

- **Tabla de ejecuciones a importar:** fecha descendente (más reciente primero)
- **Tabla de trades generados:** entryDate descendente
- **Tabla de candidatos MANUAL:** fecha descendente

### 3. Estrategia como leyenda general

- Eliminada columna "Estrategia" de la tabla de trades
- Función `buildStrategyLegend(trades)`: agrupa trades por cuenta y construye texto como "USA: FIFO · Argentina: LIFO"
- Leyenda mostrada encima de la sección de trades como texto informativo

### 4. Todo el UI en español

- Traducidos todos los headers, botones, etiquetas y placeholders:
  - "Status" → "Estado", "Side" → "Lado", "Symbol" → "Símbolo", "Entry" → "Entrada", "Exit" → "Salida"
  - "BUY" → "Compra", "SELL" → "Venta"
  - "Upload" → "Subir", pasos del wizard en español

### 5. Tabla de candidatos MANUAL — mejoras

- **Eliminada** columna "Origen"
- **Agregada** columna "Símbolo"
- **Renombrada** "ATY disp." → "Cantidad"
- **Fecha con hora:** formato `DD/MM/YYYY HH:mm:ss` via helper `fmtDateTime`
- **Filtro de candidatos:** solo se muestran BUYs con `date <= sellDate` (no se puede cerrar un trade futuro)
- Todas las columnas ordenables
- Orden default: fecha desc

---

## Tests

- `npx tsc --noEmit`: 0 errores
- `npm run test`: 42/42 tests
