# Misión: Agente UI — v23 (2026-03-23)

## Cambios en `ImportCSVView.tsx`

### Tabla de ejecuciones a importar — reordenar columnas
Nuevo orden: ID, Fecha Entrada, Símbolo, Lado, Cantidad, Precio, Broker, Cuenta
- "ID" = número secuencial (antes "#")
- Símbolo ahora va antes de Lado

### Tabla de trades a generar — reordenar + agregar columnas
Nuevo orden: F.Entrada, Símbolo, Lado, Cantidad, P.Entrada, P.Salida, F.Salida, Días, PnL $, PnL %, TNA, Estado, Broker, Cuenta
- "Lado": siempre "Compra" (todos los trades son posiciones long)
- "F. Salida": campo `exitDate` de ProjectedTrade (existe, era opcional)
- "TNA": campo `tna` de ProjectedTrade (existe, = (pnlPercent/days)*365)
- TradeRow extiende con `_exitDate: Date | null` para ser sorteable
- Limpiar el doble estado (useSortable + local useState) en TradesTable

### Tabla de trades a cerrar (Manual)
Sin cambios (prompt no especificó nada).
