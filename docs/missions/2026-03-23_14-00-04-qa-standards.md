# Agente QA — CI/CD + Verificación — 2026-03-23 v15

## Tarea 1: Actualizar .github/workflows/ci.yml
- Agregar step explícito `prisma generate`
- Reemplazar `DATABASE_URL: "file:./dev.db"` por las nuevas vars con valores mock para CI
- Los tests unitarios (Vitest) no usan Prisma → no necesitan DB real
- El build no requiere conexión real → usar URLs mock

## Tarea 2: Verificación
- npx tsc --noEmit → 0 errores
- npm run lint → 0 errores
