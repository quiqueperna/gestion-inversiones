# Orquestador — Migración Supabase + Vercel Serverless — 2026-03-23 v15

## Objetivo
Adaptar la arquitectura actual (Prisma+SQLite, sin auth) a un entorno Serverless (Vercel + Supabase PostgreSQL) sin romper la lógica de negocio ni el tipado estricto.

## Agentes y orden de ejecución

| Orden | Agente | Tarea |
|---|---|---|
| 1 | Infra/DB | schema.prisma → PostgreSQL + pooling; db.ts serverless; .env.example |
| 2 | Lógica/API | Supabase utils (server/client); middleware.ts; Server Action ejemplo migrado |
| 3 | QA | CI/CD update; tsc; lint |

## Restricción crítica
- NO tocar componentes UI ni lógica de negocio (data-loader, calculations, etc.)
- Solo capa de acceso a datos y autenticación
