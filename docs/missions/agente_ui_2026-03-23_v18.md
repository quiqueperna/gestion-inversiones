# Misión: Agente UI/UX — v18 (2026-03-23)

## Objetivo
Actualizar todos los componentes y la página principal para usar los nuevos nombres `Account`/`account`.

## Dependencia
Requiere que Agente Lógica haya actualizado data-loader y transactions.

## Tareas

### `src/app/page.tsx`
- Import: funciones renombradas (`getMemoryAccounts`, `addMemoryAccount`, etc.)
- `cuentas` state → `accounts`
- `selectedCuentas` → `selectedAccounts`
- `cuentaFilters` → `accountFilters`
- `setCuentaFilters` → `setAccountFilters`
- `editDraft.cuenta` → `editDraft.account`
- `cashFlow.cuenta` → `cashFlow.account`
- Todas las referencias en JSX

### `src/components/cuentas/CuentasSection.tsx`
- Import `Cuenta` → `Account`
- Props e interfaces internas renombradas

### `src/components/cashflow/CashFlowForm.tsx`
- `cuenta` field → `account` en form data

### `src/components/trades/TradeForm.tsx`
- Verificar si usa `cuentas` o `Cuenta`

### `src/components/ui/ViewDetailModal.tsx`
- Verificar referencias

### `src/components/dashboard/YieldsGrid.tsx`
- Verificar referencias a `cuentas`

## Reglas aplicables (UI.md)
- Los textos visibles en la UI pueden mantenerse en español ("Cuenta") — solo cambian los identificadores de código
- No cambiar labels/placeholders visibles al usuario, solo los nombres de variables/funciones/tipos
