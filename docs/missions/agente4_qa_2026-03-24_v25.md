# Agente 4 — QA y Estándares
**Sesión:** v25 | **Fecha:** 2026-03-24

## Objetivo
Verificar que el aislamiento por userId funciona y que no hay regresiones.

## Checklist
1. `npx tsc --noEmit` → 0 errores
2. `npm run lint` → 0 errores en archivos nuevos/modificados
3. `npm run test` → 42 tests passing (tests usan '_test_' userId, no DB)
4. Verificar que Account/Broker usan `@@unique([userId, nombre])` correctamente
5. Verificar que ensureDataLoaded() retorna solo datos del usuario autenticado
6. Verificar que resetMemoryState() recibe userId en todos los call sites

## Reglas
- Tests no deben modificarse: usan initializeMemoryState() sin userId → default '_test_'
- Todos los errores TypeScript deben corregirse antes de cerrar sesión
