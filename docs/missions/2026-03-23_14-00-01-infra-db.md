# Agente Infra/DB — Migración Supabase — 2026-03-23 v15

## Tarea 1: schema.prisma → PostgreSQL + Connection Pooling
- provider: `sqlite` → `postgresql`
- url: `env("POSTGRES_PRISMA_URL")` — puerto 6543 modo Transaction (pooling)
- directUrl: `env("POSTGRES_URL_NON_POOLING")` — puerto 5432 para migraciones

## Tarea 2: src/server/db.ts → Singleton serverless optimizado
- Agregar `datasourceUrl: process.env.POSTGRES_PRISMA_URL` en el constructor
- El patrón `globalForPrisma` ya evita múltiples instancias en dev hot-reload

## Tarea 3: .env.example
Crear con todas las variables necesarias documentadas:
- POSTGRES_PRISMA_URL (pooling :6543)
- POSTGRES_URL_NON_POOLING (directo :5432)
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- DATABASE_URL (legacy local SQLite)
