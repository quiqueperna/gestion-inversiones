# Misión: Agente UI — v20 (2026-03-23)

## Objetivo
Rediseñar ImportCSVView con dos tablas separadas y validación BD.

## Cambios en `src/components/trades/ImportCSVView.tsx`
- Agregar props `brokers: string[]` y `accounts: string[]`
- Pasar a `parseImportCSV` para validación
- Separar rows en `validRows` e `invalidRows`
- Mostrar **Tabla 1**: filas válidas (preview verde/normal)
- Mostrar **Tabla 2**: filas inválidas con columna "Razón" al final (rojo)
- Botón confirmar solo activo si hay validRows

## Cambios en `src/app/page.tsx`
- Pasar `brokers={brokers.map(b => b.nombre)}` y `accounts={accounts.map(c => c.nombre)}` a `ImportCSVView`
