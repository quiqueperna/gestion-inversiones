# Misión: Agente UI — v22 (2026-03-23)

## Cambios en `ImportCSVView.tsx`

### Estrategia — leyenda general (no columna)
- Eliminar columna "Estrategia" de la tabla de trades
- Mostrar leyenda general encima del CollapsibleSection de trades:
  "Estrategia de cierre: {estrategia}" → pero como puede haber múltiples cuentas con distintas estrategias, mostrar: "Estrategias: {cuenta1}: FIFO · {cuenta2}: LIFO ..."
  - Las estrategias se derivan de los trades generados (field `trade.strategy` + `trade.account`)

### Orden por defecto
- Tabla de ejecuciones: ordenada por fecha desc
- Tabla de trades: ordenada por entryDate desc

### Columnas ordenables (SortableTable)
Crear componente/hook `useSortableTable<T>` que:
- Recibe array de items + columnas con accessor
- Mantiene estado { col, dir: 'asc'|'desc' }
- Header clickable: muestra ▲▼ o flecha activa
- Aplicar a ExecsTable y TradesTable

### Todo en español
Revisar y traducir: "Status"→"Estado", "Side"→"Lado", "Symbol"→"Símbolo", "Entry"→"Entrada", "Exit"→"Salida",
headers, botones, etiquetas, placeholders

### Tabla de candidatos MANUAL — cambios
- Eliminar columna "Origen"
- Agregar columna "Símbolo"
- Renombrar "ATY disp." → "Cantidad"
- Mostrar fecha Y hora en columna fecha (formato DD/MM/YYYY HH:mm:ss)
- Filtrar candidatos: solo mostrar BUYs con date <= fecha del SELL (no se puede cerrar un trade abierto despues)
- Orden por defecto: fecha desc
- Todas las columnas ordenables
