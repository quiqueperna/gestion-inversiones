# Agente UI/UX — 2026-03-23 v14

## Tarea 1: Mover "Sistema Online" al navbar

**Archivo:** `src/app/page.tsx`
- Eliminar el bloque `{view !== "movimientos" && (<div>..Sistema Online..</div>)}` del FilterBar (línea ~823).
- Agregar el indicador en el `<div className="flex items-center gap-2">` del nav (línea ~738), siempre visible.

## Tarea 2: Sidebar de Configuraciones

**Secciones** (según config.md):
- **CUENTA**: Mi cuenta (User), Mi plan (Trophy), Zona de peligro (AlertCircle)
- **PREFERENCIAS**: Apariencia (Palette), Idioma (Languages), Regional (Globe), Dashboard (BarChart3)
- **ALERTAS**: Alertas (Bell)

**Layout:** flex row — sidebar izquierdo fijo + panel de contenido derecho.
**Active state:** item resaltado con `bg-white/10 text-white`, resto `text-zinc-500 hover:text-zinc-300`.
**Contenido activo:**
- `dashboard`: mostrar la lógica existente de Estrategia de Matching.
- Resto: placeholder "Próximamente" con icono y descripción breve.

**State nuevo:** `const [configSection, setConfigSection] = useState("mi-cuenta")`

**Iconos a importar:** `User, Globe, Bell, Palette, Languages, BarChart3` desde lucide-react.

## Reglas
- No cambiar lógica de negocio.
- TypeScript strict, sin `any` nuevo.
- Mobile: sidebar collapsa (usar clase `hidden md:flex` para el sidebar en mobile).
