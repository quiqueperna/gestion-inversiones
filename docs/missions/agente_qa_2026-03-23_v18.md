# Misión: Agente QA y Estándares — v18 (2026-03-23)

## Objetivo
Verificar que el rename de Cuenta→Account no rompe nada.

## Dependencia
Requiere que los 3 agentes anteriores hayan terminado.

## Checklist

1. `npx tsc --noEmit` → debe dar 0 errores
2. `npm run test` → debe pasar 42/42 tests
3. `npm run lint` → sin errores
4. `npm run build` → build limpio (opcional si los anteriores pasan)

## Puntos de atención
- Que no queden referencias a `db.cuenta.*` (Prisma ya no lo reconocerá tras la migración)
- Que no queden imports de `Cuenta` sin actualizar
- Que `cashFlow.cuenta` esté completamente renombrado a `cashFlow.account`
- Que los tests no fallen por cambio de nombres de exports en transactions.ts
