# Bitácora de Prompts

## 24 de Marzo, 2026 — Sesión v24

### Prompt 1
> "Implementar un sistema de autenticación completo usando la librería oficial @supabase/ssr. Por favor, genera el código para:
> Middleware de Next.js: Para refrescar la sesión del usuario en cada petición y proteger rutas privadas (redirigir al /login si no hay sesión).
> Server Actions: Un archivo actions.ts con funciones para login, signup (con confirmación de email) y logout.
> Client Component: Un formulario de login sencillo que use estas Server Actions.
> Configuración del Cliente: El archivo utils/supabase/server.ts y client.ts siguiendo la documentación actual de SSR de Supabase.
> Usa TypeScript y asegúrate de explicar cómo configurar las variables de entorno NEXT_PUBLIC_SUPABASE_URL y NEXT_PUBLIC_SUPABASE_ANON_KEY.
> Agregale la posibilidad de autenticar con google."

**Resultado:** Implementación completa. Se crearon:
- `src/server/actions/auth.ts` (login, signup, logout, loginWithGoogle)
- `src/app/login/page.tsx` (formulario con toggle login/signup + Google OAuth)
- `src/app/auth/callback/route.ts` (handler de OAuth callback)
- `src/middleware.ts` actualizado con protección de rutas privadas

Los clientes `server.ts` y `client.ts` ya existían y estaban correctamente implementados.
TypeScript: 0 errores | Tests: 42/42 passing.
