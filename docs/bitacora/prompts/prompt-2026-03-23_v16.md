# Bitácora de Prompts

## 23 de Marzo, 2026 — Sesión v16

### Prompt 1
> "Por defecto el proyecto corre en Modo Demo (datos en memoria desde CSV). Quiero que actives la persistencia, recorda que migre a supabase, ya tengo todo el ambiente local preparado, la base de datos creada y sus tablas.
>
> No tienen datos, asi que generame algunos datos de pruebas cargados en la BD."

**Resultado:**
Persistencia activada con éxito. El sistema ahora carga datos desde Supabase/Prisma al iniciar (con fallback a CSV si la BD está vacía). Se generaron 341 executions, 201 trade units y 9 cashflows en la BD local. Todas las mutaciones (create/delete/update de executions, tradeunits y cashflows) ahora persisten en Supabase. TypeScript OK, 42/42 tests, build limpio.
