# Bitácora de Prompts

## 24 de Marzo, 2026 — Sesión v26

### Prompt 1
> "tengo un error al querer guardar datos. Unique constraint failed on the fields: (`nombre`)"

**Resultado:** Fix aplicado. La migración v25 usaba `DROP CONSTRAINT` pero el índice único fue creado como `CREATE UNIQUE INDEX` (no como CONSTRAINT), por lo que el DROP no tuvo efecto. Se ejecutó `DROP INDEX IF EXISTS "Account_nombre_key"` y `DROP INDEX IF EXISTS "Broker_nombre_key"` directamente en la DB. Tests: 42/42 passing.
