# Bitácora de Prompts

## 24 de Marzo, 2026 — Sesión v25

### Prompt 1
> "me loguee con 2 usuarios diferentes y estoy compartiendo los datos. Los datos deben ser privados e independientes entre usuarios"

**Resultado:** Implementación completa de aislamiento por usuario. Se agregó campo `userId` a todos los modelos Prisma (Execution, TradeUnit, CashFlow, Account, Broker), se convirtió el `memoryState` singleton en un `Map<userId, MemState>`, y se filtraron todas las queries DB por `userId`. Tests: 42/42 passing. TypeScript: 0 errores.
