## Flujo de trabajo
- Crear branch por feature.
- Implementar por slices verticales (UI + API + DB).
- No cerrar tarea sin pasar `lint + typecheck + test + build`.
- Registrar aprendizaje en `bitacora/lessons.md` solo si hubo error repetible.